/*
Persona
Declara una tupla Usuario con los campos siguientes:
el nombre y dos apellidos (todos strings), la edad (un entero),
el teléfono (un entero), y si es un usuario administrador (un Booleano).
 */

#include <iostream>
using namespace std;
struct Usuario{
  string nombre, apellido_p, apellido_m;
  int edad, telefono;
  bool administrador;
};
int main(){

}